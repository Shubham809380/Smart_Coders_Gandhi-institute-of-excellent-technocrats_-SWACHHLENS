# === ENV / SECRETS ===
.env
.env.local
.env.*
!.env.example
!.env.example.frontend

# === NODE ===
node_modules/
dist/
.vite/

# === PYTHON ===
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
.eggs/
venv/
.venv/
env/

# === AI MODEL CHECKPOINTS (large binary files) ===
swachhlens-ai/checkpoints/**
# ...except the small CNN classifier artifacts required by the deployed service
!swachhlens-ai/checkpoints/best_classifier.pth
!swachhlens-ai/checkpoints/best_classifier.onnx
!swachhlens-ai/checkpoints/onnx_meta.json
!swachhlens-ai/checkpoints/thresholds.json
!swachhlens-ai/checkpoints/eval_report.json
!swachhlens-ai/checkpoints/training_summary.json

# === TRAINING DATA (large datasets, not for repo) ===
swachhlens-ai/training/images/
swachhlens-ai/training/labels/
swachhlens-ai/training/runs/
swachhlens-ai/training/runs/**

# === FIREBASE ===
.firebase/
firebase-debug.log

# === BUILD ARTIFACTS ===
backend/data/state.json
backend/uploads/
*.log

# === OS / EDITOR ===
.DS_Store
Thumbs.db
Desktop.ini
.vscode/
.idea/
*.swp
*.swo
*~

.vercel
.env*

# === ML TRAINING (raw datasets too large for git) ===
garbage_classification/
realwaste-main/
swachhlens-ai/training/cifar10/
swachhlens-ai/training/*.log
